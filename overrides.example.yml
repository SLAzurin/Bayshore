# This is an example of what overrides.yml could look like
# In this case, when running docker compose -f docker-compose.yml -f overrides.yml up -d,
# the exposed ports of the bayshore container will be 8080:80, 10082:10082, and 9002:9002 instead of 80:80, 10082:10082, and 9002:9002

# This file is completely optional.

services:
  bayshore:
    ports:
      - 8080:80
      - 10082:10082
      - 9002:9002
